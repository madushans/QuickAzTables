{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // if you have a local emulator (i.e Azurite) you can use it by setting connection string to "UseDevelopmentStorage=true"
    // For guidance about Azurite see https://learn.microsoft.com/en-us/azure/storage/common/storage-use-azurite?tabs=visual-studio
    // Or use a connection string from a real azure storage subscription.
    "TableStorage": "add your connection string here"
  },

  "AllowedHosts": "*"
}
